#!/usr/bin/env ruby
# frozen_string_literal: true

libdir = File.expand_path('File.dirname(__FILE__)/../lib')
$LOAD_PATH.unshift(libdir) unless $LOAD_PATH.include?(libdir)

require 'word_search'

WordSearch::Application.new(File.read(ARGV.shift)).run
